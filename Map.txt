#include "stdafx.h"
#include <iostream>
#include <conio.h>
using namespace std;

class Mapa 
{
public:
	int player_x;
	int player_y;
	int map_width;
	int map_height;
	void set_player(int x, int y)
	{
		player_x = x;
		player_y = y;
	}
	void set_map(int width, int height)
	{
		map_width = width;
		map_height = height;
	}

	void Plansza(int map_width, int map_height, int player_x, int player_y)
	{
		system("cls");															//resetuje poprzednie po³o¿enie planszy
		for (int i = 0; i < map_height; i++)										//pêtla odpowaidaj¹ca za wiersze w trakcie iteracji
		{
			for (int j = 0; j < map_width; j++)									//pêtla odpowiadaj¹ca za kolumny, kolejne pola mapy w danym wierszu
			{
				if (j == 0 || j == map_width - 1 || i == 0 || i == map_height - 1)  //warunek tworz¹cy ramkê z #
					cout << "#";
				else if (i == player_x && j == player_y)										//warunek umieszczaj¹cy X
				{
					cout << "X";

				}
				else															//warunek wype³niaj¹cy resztê planszy pustk¹
					cout << " ";
			}
			cout << "\n";
		}
	}
};